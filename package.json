{
  "name": "shah-mobile-workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @workspace/shah-mobile run dev",
    "build": "pnpm --filter @workspace/shah-mobile run build",
    "typecheck": "tsc --build --emitDeclarationOnly"
  }
}
